name: Super Linter

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  packages: read
  statuses: write

jobs:
  lint:
    name: Super Linter
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .tool-versions
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Lint Codebase
        uses: super-linter/super-linter/slim@v8
        env:
          DEFAULT_BRANCH: main
          FILTER_REGEX_EXCLUDE: dist/**/*
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LINTER_RULES_PATH: .github/config/linter
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_BIOME_FORMAT: false # repo uses oxfmt
          VALIDATE_BIOME_LINT: false # repo uses oxlint
          VALIDATE_CHECKOV: false # TODO: Add checkov config file!
          VALIDATE_JAVASCRIPT_ES: false
          VALIDATE_JAVASCRIPT_PRETTIER: false # repo uses oxfmt
          VALIDATE_JSCPD: false
          VALIDATE_JSON: false
          VALIDATE_JSON_PRETTIER: false # repo uses oxfmt
          VALIDATE_JSONC_PRETTIER: false # repo uses oxfmt
          VALIDATE_TYPESCRIPT_ES: false
          VALIDATE_TYPESCRIPT_PRETTIER: false # repo uses oxfmt
          VALIDATE_YAML_PRETTIER: false # repo uses oxfmt
